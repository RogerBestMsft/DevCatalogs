$schema: "1.0"
name: "devbox-data-analytics-ai"
image: microsoftvisualstudio_visualstudio2019plustools_vs-2019-pro-general-win11-m365-gen2
description: "TEST"

tasks:
  - name: ~/powershell
    parameters:
      command: |
        # Retrieve secrets from Azure Key Vault
        mkdir c:\roger
        $xxx = 'boo'
        $xxx | Out-File -FilePath 'C:\roger\bbb.txt'
        # 3
        $test = '{{https://rebabhgh4exvcql44e.privatelink.vaultcore.azure.net/secrets/TestSecret}}'
        $test | Out-File -FilePath "C:\roger\aaa.txt" -Encoding UTF8
