# yaml-language-server: $schema=https://github.com/Azure/deployment-environments/releases/download/2022-11-11-preview/manifest.schema.json
name: AddPrivateDeployScript
version: 1.0.0
summary: Use deploy script to access private storage account.
description: Use deploy script to access private storage account.
runner: ARM
templatePath: azuredeploy.json

parameters: 
- id: "prefix"
  name: "prefix"
  type: "string"
  required: true
